# Локально для npm run seed (из Vercel → Storage → Blob → .env.local)
BLOB_READ_WRITE_TOKEN=

# Пароль добавления/удаления на сервере (по умолчанию tinytiny)
ADMIN_PASSWORD=tinytiny

# Cookies одной строкой для импорта с OK.ru на Vercel (опционально)
OK_COOKIES=

# Лимит фото за один импорт (Hobby: лучше 30–50)
IMPORT_MAX=80
