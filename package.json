{
  "name": "ok-photo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "seed": "SEED_BLOB=1 node scripts/seed-blob.mjs",
    "build": "echo 'skip auto-seed — данные только в Blob'"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.3"
  },
  "devDependencies": {
    "vercel": "^41.6.0"
  }
}
